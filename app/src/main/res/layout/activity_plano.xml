<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_plano.xml
    Plano interactivo de la CN Almaraz.
    Los botones de zona se generan dinámicamente en PlanoActivity.crearBotonesZona()
    y se posicionan encima del ImageView usando PlanoCoordMapper.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/mapaContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light">

    <!-- Fondo del plano. scaleType fitCenter es OBLIGATORIO para que
         PlanoCoordMapper calcule las posiciones correctamente. -->
    <ImageView
        android:id="@+id/planoImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:adjustViewBounds="true"
        android:contentDescription="Plano esquemático CN Almaraz"
        android:scaleType="fitCenter"
        android:src="@drawable/plano_almaraz" />

    <!-- Los botones de zona se añaden aquí desde código -->

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabVolver"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="12dp"
        android:contentDescription="Volver"
        app:backgroundTint="@color/nuclear_blue"
        app:fabSize="mini"
        app:srcCompat="@drawable/ic_arrow_back" />

</FrameLayout>
